import streamlit as st

st.title("Predictive power")
st.markdown(
    """
Planned section: correlations / simple lead-lags between macro features (and commodities) vs SSS decomposition.

Placeholder until we lock the target horizons and transforms from the memo.
"""
)
